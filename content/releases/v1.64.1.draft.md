# vg v1.64.1 release notes (staged for card drafting)

Published: 2025-03-24T20:43:23Z
URL: https://github.com/vgteam/vg/releases/tag/v1.64.1

---

**[![Download for Linux](https://raw.githubusercontent.com/vgteam/vg/master/doc/figures/download-linux.png)
](https://github.com/vgteam/vg/releases/download/v1.64.1/vg)**

Don't forget to mark the static binary executable:
```
chmod +x vg
```
**Docker Image:** `quay.io/vgteam/vg:v1.64.1`

**Buildable Source Tarball:** [vg-v1.64.1.tar.gz](https://github.com/vgteam/vg/releases/download/v1.64.1/vg-v1.64.1.tar.gz)

Includes source for vg and all submodules. **Use this instead of Github's "Source Code" downloads**; those will not build as they do not include code for bundled dependencies that the `vg` build process needs.

This release includes:

* Giraffe will no longer cut nodes wrong when anchoring an extracted subgraph's end on the reverse strand of the same node as its start
* Giraffe will make fewer tips, by enforcing path length strictly for connecting subgraph extraction. Dagification will still produce tips that will still be trimmed.
* `vg giraffe` will no longer confuse the two orientations of single bounding nodes when aligning between positions, a source of invalid alignments.
* New `vg align --between POS,POS` feature for manually exercisign the align-between-positions code used by `vg giraffe`.
* vg now only builds required parts of `vcflib`
* `vgmanmd.py` now properly handles `--help` failures

## Updated Submodules

The `gbwtgraph`, `gcsa2`, `libhandlegraph`, and `libvgio` submodules have been updated.


---

## Next step

Turn the notable changes above into cards in `content/releases/v1.64.1.yaml`,
following `content/schemas/card.schema.json`. Suggested frontmatter:

```yaml
deck: vg-jargon::releases::v1.64.1
description: Notable changes in vg v1.64.1
source_repo: vg

cards:
  - id: rel-001
    type: basic
    front: ...
    back: ...
    tags: [release, v1.64.1]
    difficulty: intermediate
```

Once `v1.64.1.yaml` exists, delete this draft file.
